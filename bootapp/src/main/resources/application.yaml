spring:
  datasource:
    url: jdbc:h2:file:${user.home}/.maze/spring-boot-h2-db;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: pass
  jersey:
    application-path: /maze
  h2:
    console.enabled: true

camunda:
  bpm:
    admin-user:
      id: demo
      password: demo
    auto-deployment-enabled:
      true
    authorization:
      enabled: false
      tenant-check-enabled: false

logging:
  level:
    ROOT: INFO